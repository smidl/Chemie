# Chemie — Claude notes (thin)

Canonical instructions are in `AGENTS.md` (this folder) and `~/agents/`. This
is a coord **orchestrator** (pilot); children: `Synthesis/`, `MolGPT/`.
See `AGENTS.md` for authority rules and what this node owns.
