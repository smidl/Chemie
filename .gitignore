# Chemie coordination repo — tracks coordination + briefing + markers,
# NOT project code (those are submodules) or data.

# environments / caches
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store

# large data — never in the coordination repo
datasets/
datasets.tar.gz
*.tar.gz

# tmp / scratch
*.tmp
.tmp.*

# unenrolled / transparent siblings (kept out of the coordination repo)
RetroDemo/

# external / boundary repos — inventory-only (declared in markers, NOT tracked here)
/retrosyntesis/
/proposal/
MolGPT/MolPFN/
MolGPT/phd-proposal/

# legacy Synthesis layer — content being re-homed (Concern C), then removed
/Synthesis/

# NOTE: retro-pfn/ and briefing/ are git SUBMODULES (added explicitly).
# MolGPT/ is a tracked folder (no own git); its nested repos are ignored above.
