# MolGPT — Claude notes (thin)

Canonical instructions are in `AGENTS.md` (this folder) and `~/agents/`. This
is a coord **leaf** of the Chemie tree: a general-purpose molecule model
(GPT → PFN). It hosts the external student repo `MolPFN/` (rektomar) for
advising only — never write into it. Advisory overview: `molpfn-advice.md`.
